# Judge issue #1432

## The task, as written

trios/agent-server/apps/server/src/lib/container/managed/types.ts breaks L3 with 6 non-ASCII characters, all of them in comments

## The criteria, one per line

1. `LC_ALL=C grep -cP '[^\x00-\x7F]' trios/agent-server/apps/server/src/lib/container/managed/types.ts` prints 0, and the raw output is quoted in the report. The command MUST NOT name or enumerate the specific items it counts.
2. `bun run typecheck 2>&1 | grep -c trios/agent-server/apps/server/src/lib/container/managed/types.ts` prints 0, and the raw output is quoted. The command MUST NOT name or enumerate the specific items it counts.
3. The report quotes `git diff --stat` for the branch, showing exactly one file changed.
4. The report quotes one before-and-after comment line, so a reader can see the substitution that was made.

## What the bee changed

```
.../apps/server/src/lib/container/managed/types.ts           | 12 ++++++------
 1 file changed, 6 insertions(+), 6 deletions(-)
```

## The diff, in full

```diff
diff --git a/trios/agent-server/apps/server/src/lib/container/managed/types.ts b/trios/agent-server/apps/server/src/lib/container/managed/types.ts
index 5c1eda4ff..6b8c2dab5 100644
--- a/trios/agent-server/apps/server/src/lib/container/managed/types.ts
+++ b/trios/agent-server/apps/server/src/lib/container/managed/types.ts
@@ -8,7 +8,7 @@
  * focused on behaviour.
  */
 
-/** Subset of `NodeJS.Platform` — kept loose so a future Linux add
+/** Subset of `NodeJS.Platform` - kept loose so a future Linux add
  *  doesn't require touching the abstraction. */
 export type Platform = NodeJS.Platform
 
@@ -23,7 +23,7 @@ export type ContainerState =
   | 'errored'
 
 /** Subset of the state machine that callers should treat as "may
- *  resolve to running on its own with time" — `execProcess` and
+ *  resolve to running on its own with time" - `execProcess` and
  *  friends wait through these. Callers checking `isReady()` use this. */
 export const TRANSIENT_STATES: ReadonlySet<ContainerState> = new Set([
   'installing',
@@ -49,7 +49,7 @@ export interface ContainerDescriptor {
 }
 
 /**
- * Mount root the container exposes. `kind` is informational —
+ * Mount root the container exposes. `kind` is informational -
  * `'shared'` mounts are bind-mounted once and used by every agent;
  * `'per-agent'` mounts are bind-mounted per agent and the harness's
  * agent-delete flow is expected to clean them up. Both are
@@ -71,7 +71,7 @@ export interface ContainerStatusSnapshot {
 
 /** What the caller wants to run inside the container. */
 export interface ExecSpec {
-  /** Argv inside the container — first element is the executable. */
+  /** Argv inside the container - first element is the executable. */
   argv: readonly [string, ...string[]]
   env?: Record<string, string>
 }
@@ -82,11 +82,11 @@ export interface ExecResult {
   stderr: string
 }
 
-/** Reset granularity — see the architecture doc / team brief. */
+/** Reset granularity - see the architecture doc / team brief. */
 export type ResetLevel = 'soft' | 'wipe-agent' | 'hard'
 
 export interface ResetOptions {
-  /** Required for `level === 'wipe-agent'` — agent id whose home dir
+  /** Required for `level === 'wipe-agent'` - agent id whose home dir
    *  should be removed. Ignored for soft/hard. */
   agentId?: string
   onLog?: (msg: string) => void
```

## The bee said nothing that was recorded, so every quoted-run criterion is UNVERIFIABLE by absence



