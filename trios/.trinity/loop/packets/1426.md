# Judge issue #1426

## The task, as written

trios/agent-server/apps/server/src/lib/agents/runtime/agent-runtime.ts breaks L3 with 160 non-ASCII characters, all of them in comments

## The criteria, one per line

1. `LC_ALL=C grep -cP '[^\x00-\x7F]' trios/agent-server/apps/server/src/lib/agents/runtime/agent-runtime.ts` prints 0, and the raw output is quoted in the report. The command MUST NOT name or enumerate the specific items it counts.
2. `bun run typecheck 2>&1 | grep -c trios/agent-server/apps/server/src/lib/agents/runtime/agent-runtime.ts` prints 0, and the raw output is quoted. The command MUST NOT name or enumerate the specific items it counts.
3. The report quotes `git diff --stat` for the branch, showing exactly one file changed.
4. The report quotes one before-and-after comment line, so a reader can see the substitution that was made.

## What the bee changed

```
.../apps/server/src/lib/agents/runtime/agent-runtime.ts        | 10 +++++-----
 1 file changed, 5 insertions(+), 5 deletions(-)
```

## The diff, in full

```diff
diff --git a/trios/agent-server/apps/server/src/lib/agents/runtime/agent-runtime.ts b/trios/agent-server/apps/server/src/lib/agents/runtime/agent-runtime.ts
index 2635fd923..ae333cdfe 100644
--- a/trios/agent-server/apps/server/src/lib/agents/runtime/agent-runtime.ts
+++ b/trios/agent-server/apps/server/src/lib/agents/runtime/agent-runtime.ts
@@ -21,27 +21,27 @@ import type {
 export interface AgentRuntime {
   readonly descriptor: RuntimeDescriptor
 
-  // ── Status surface (Plane B feed) ────────────────────────────────
+  // -- Status surface (Plane B feed) --------------------------------
   getStatusSnapshot(): RuntimeStatusSnapshot
   subscribe(listener: StateListener): Unsubscribe
   getCapabilities(): ReadonlyArray<RuntimeCapability>
 
-  // ── Action dispatch (Plane B control) ────────────────────────────
+  // -- Action dispatch (Plane B control) ----------------------------
   executeAction(
     action: RuntimeAction,
     options?: { onLog?: (msg: string) => void },
   ): Promise<void>
 
-  // ── ACP plane integration ────────────────────────────────────────
+  // -- ACP plane integration ----------------------------------------
   /**
    * Build the shell-command string acpx-core spawns to run `spec`
    * against this runtime. For container kinds, this is the
-   * `limactl shell <vm> -- nerdctl exec -i …` chain; for host kinds,
+   * `limactl shell <vm> -- nerdctl exec -i ...` chain; for host kinds,
    * it's `env KEY=VAL <binary> <argv...>`.
    */
   buildExecArgv(spec: ExecSpec): string
 
-  // ── Filesystem ───────────────────────────────────────────────────
+  // -- Filesystem ---------------------------------------------------
   /** Per-agent home dir on host. Both kinds expose this; container
    *  kinds also expose `toContainerPath` for in-container translation. */
   getPerAgentHomeDir(agentId: string): string
```

## The bee said nothing that was recorded, so every quoted-run criterion is UNVERIFIABLE by absence



